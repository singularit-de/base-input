import type {InputClasses} from '../interface'

const defaultTheme: InputClasses = {
  wrapper: 'flex shadow appearance-none border rounded px-3 cursor-text',
  input: 'leading-tight focus:outline-none bg-transparent h-full',
  prefix: 'h-full items-center pr-2',
  suffix: 'h-full items-center pl-2',
}

export default defaultTheme
